package com.aidebar.greendaotest.gen;

import android.database.Cursor;
import android.database.sqlite.SQLiteStatement;

import org.greenrobot.greendao.AbstractDao;
import org.greenrobot.greendao.Property;
import org.greenrobot.greendao.internal.DaoConfig;
import org.greenrobot.greendao.database.Database;
import org.greenrobot.greendao.database.DatabaseStatement;

import com.china.snapshot.bean.HttpBeanMediaLikeDetail;

// THIS CODE IS GENERATED BY greenDAO, DO NOT EDIT.
/** 
 * DAO for table "HTTP_BEAN_MEDIA_LIKE_DETAIL".
*/
public class HttpBeanMediaLikeDetailDao extends AbstractDao<HttpBeanMediaLikeDetail, String> {

    public static final String TABLENAME = "HTTP_BEAN_MEDIA_LIKE_DETAIL";

    /**
     * Properties of entity HttpBeanMediaLikeDetail.<br/>
     * Can be used for QueryBuilder and for referencing column names.
     */
    public static class Properties {
        public final static Property UserName = new Property(0, String.class, "userName", true, "USER_NAME");
        public final static Property MediaUrl = new Property(1, String.class, "mediaUrl", false, "HttpBeanMediaLikeDetail");
        public final static Property IsLike = new Property(2, boolean.class, "isLike", false, "IS_LIKE");
    }


    public HttpBeanMediaLikeDetailDao(DaoConfig config) {
        super(config);
    }
    
    public HttpBeanMediaLikeDetailDao(DaoConfig config, DaoSession daoSession) {
        super(config, daoSession);
    }

    /** Creates the underlying database table. */
    public static void createTable(Database db, boolean ifNotExists) {
        String constraint = ifNotExists? "IF NOT EXISTS ": "";
        db.execSQL("CREATE TABLE " + constraint + "\"HTTP_BEAN_MEDIA_LIKE_DETAIL\" (" + //
                "\"USER_NAME\" TEXT PRIMARY KEY NOT NULL ," + // 0: userName
                "\"HttpBeanMediaLikeDetail\" TEXT," + // 1: mediaUrl
                "\"IS_LIKE\" INTEGER NOT NULL );"); // 2: isLike
    }

    /** Drops the underlying database table. */
    public static void dropTable(Database db, boolean ifExists) {
        String sql = "DROP TABLE " + (ifExists ? "IF EXISTS " : "") + "\"HTTP_BEAN_MEDIA_LIKE_DETAIL\"";
        db.execSQL(sql);
    }

    @Override
    protected final void bindValues(DatabaseStatement stmt, HttpBeanMediaLikeDetail entity) {
        stmt.clearBindings();
 
        String userName = entity.getUserName();
        if (userName != null) {
            stmt.bindString(1, userName);
        }
 
        String mediaUrl = entity.getMediaUrl();
        if (mediaUrl != null) {
            stmt.bindString(2, mediaUrl);
        }
        stmt.bindLong(3, entity.getIsLike() ? 1L: 0L);
    }

    @Override
    protected final void bindValues(SQLiteStatement stmt, HttpBeanMediaLikeDetail entity) {
        stmt.clearBindings();
 
        String userName = entity.getUserName();
        if (userName != null) {
            stmt.bindString(1, userName);
        }
 
        String mediaUrl = entity.getMediaUrl();
        if (mediaUrl != null) {
            stmt.bindString(2, mediaUrl);
        }
        stmt.bindLong(3, entity.getIsLike() ? 1L: 0L);
    }

    @Override
    public String readKey(Cursor cursor, int offset) {
        return cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0);
    }    

    @Override
    public HttpBeanMediaLikeDetail readEntity(Cursor cursor, int offset) {
        HttpBeanMediaLikeDetail entity = new HttpBeanMediaLikeDetail( //
            cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0), // userName
            cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1), // mediaUrl
            cursor.getShort(offset + 2) != 0 // isLike
        );
        return entity;
    }
     
    @Override
    public void readEntity(Cursor cursor, HttpBeanMediaLikeDetail entity, int offset) {
        entity.setUserName(cursor.isNull(offset + 0) ? null : cursor.getString(offset + 0));
        entity.setMediaUrl(cursor.isNull(offset + 1) ? null : cursor.getString(offset + 1));
        entity.setIsLike(cursor.getShort(offset + 2) != 0);
     }
    
    @Override
    protected final String updateKeyAfterInsert(HttpBeanMediaLikeDetail entity, long rowId) {
        return entity.getUserName();
    }
    
    @Override
    public String getKey(HttpBeanMediaLikeDetail entity) {
        if(entity != null) {
            return entity.getUserName();
        } else {
            return null;
        }
    }

    @Override
    public boolean hasKey(HttpBeanMediaLikeDetail entity) {
        return entity.getUserName() != null;
    }

    @Override
    protected final boolean isEntityUpdateable() {
        return true;
    }
    
}
